<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:background="@android:color/transparent">

    <!-- Left: Caller avatar with initial -->
    <ImageView
        android:id="@+id/caller_avatar"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:scaleType="centerCrop"
        android:layout_marginEnd="12dp"
        android:contentDescription="Caller avatar" />

    <!-- Middle: Caller info (takes remaining space) -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginEnd="12dp">

        <!-- Title: Caller name -->
        <TextView
            android:id="@+id/caller_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:textColor="#FFFFFF"
            android:fontFamily="sans-serif-medium"
            android:ellipsize="end"
            android:maxLines="1" />

        <!-- Subtitle: Call duration (Chronometer for automatic ticking) -->
        <Chronometer
            android:id="@+id/call_chronometer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="#DDDDDD"
            android:layout_marginTop="1dp"
            android:format="%s" />

        <!-- Held caller info (hidden when no held call) -->
        <TextView
            android:id="@+id/held_caller_info"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#AAAAAA"
            android:layout_marginTop="2dp"
            android:visibility="gone"
            android:ellipsize="end"
            android:maxLines="1" />

    </LinearLayout>

    <!-- Swap button (hidden by default, shown when there's a held call) -->
    <FrameLayout
        android:id="@+id/swap_button"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginEnd="10dp"
        android:background="@drawable/ic_swap_button_bg"
        android:contentDescription="Swap calls"
        android:visibility="gone">

        <ImageView
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_swap_calls"
            android:scaleType="fitCenter" />

    </FrameLayout>

    <!-- Red hangup button with phone icon -->
    <FrameLayout
        android:id="@+id/hangup_button"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="@drawable/ic_hangup_red_circle"
        android:contentDescription="End call">

        <ImageView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_call_end_white"
            android:scaleType="fitCenter" />

    </FrameLayout>

</LinearLayout>
